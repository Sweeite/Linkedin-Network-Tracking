import { PlaceholderPage } from "@/components/placeholder-page";

export default function PostsPage() {
  return (
    <PlaceholderPage
      title="Posts"
      description="LinkedIn post performance — CRUD + views-over-time chart."
    />
  );
}
