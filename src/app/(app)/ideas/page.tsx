import { PlaceholderPage } from "@/components/placeholder-page";

export default function IdeasPage() {
  return (
    <PlaceholderPage
      title="Content ideas"
      description="Brainstorm export + idea tracking from new → drafted → posted."
    />
  );
}
